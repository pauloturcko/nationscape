import styled from "styled-components";

export const FilterDataSelect = () => {
  return <StyledSelect />;
};

const StyledSelect = styled.select`
  width: 80%;
  height: auto;
  font-size: 1rem;
`;
